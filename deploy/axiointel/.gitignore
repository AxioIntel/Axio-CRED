# Local to the collector's machine: the places it collects, and AxioIntel's own programs,
# fetched from Axio-Backend so they never drift from the endpoints they talk to.
places.txt
push_native.py
fetch_targets.py
